-- Postgres
SELECT 
    id "User Identification Number" 
FROM user;

-- MySQL
SELECT 
    id "User Identification Number"
FROM user;

SELECT 
    id `User Identification Number`
FROM user;

SELECT 
    id 'User Identification Number'
FROM user;

